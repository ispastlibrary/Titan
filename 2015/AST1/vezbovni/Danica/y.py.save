import numpy as np
import matplotlib.pyplot as plt
x=np.arange(0,10,0.1)

def funkcija(x,y):
   return -np.cos(x)
dx=0.1
x0=0
y0=0
nizx=[]
nizy=[]

for i in range(len(x)):
#seta mi po x osi, po dx    
    k=funkcija(x,y)
    y=y0+k*dx
    x=x0+dx
    x0=x
    y0=y
    nizx.append(x)
#treba mi neki pomocni niz u koji cu da smestim sve moje x-eve, treba mi to zbog lakseg plotovanja
    nizy.append(y)
#isto kao i za nizy
k=(
    
    
    
    




plt.plot(x,k)
plt.show()


