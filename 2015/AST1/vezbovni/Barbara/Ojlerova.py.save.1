import numpy as np
import matplotlib.pyplot as plt

dx = 0.3

def fun(x,y):    
    return -np.cos(x)

y0=0
x0=0
x = x0
#z=fun(x0,y0)
#print(z)
y=y0
n = 10
f=[] 
xn = []
xn.append(x)
f.append(y0)
for i in range(n):    
    y=y+dx*fun(x,y)
    x += dx
    xn.append(x)    
#    print(y)    
    f.append(y)
plt.plot(xn,f) 
plt.show()  
  

